#include<bits/stdc++.h>
using namespace std;
#define optimize() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);

int main()
{
optimize();

int tc;
cin >> tc;
while (tc--)
{
    int a, b;
    cin >> a >> b;

    int c = b-a;
    cout << c << endl;

    /*
ayta k ay way teu kora jay

int c = a+b/2;
int result = (c−a)+(b−c);
cout << result << endl;

*/
}

    return 0;
}
