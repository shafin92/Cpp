#include<bits/stdc++.h>
using namespace std;
#define optimize() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);

int main()
{
optimize();

string s;
cin >>s;
vector<string> s;

for (string i = 'a'; i < 'z'; i++)
{
    if (s.)
    {
        /* code */
    }
    
}

    return 0;
}